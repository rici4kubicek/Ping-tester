[project]
name = "ping-tester"
version = "0.1.0"
description = "Raspberry Pi web app for pinging a range of local IP addresses"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
    "flask-socketio>=5.3",
    "simple-websocket>=1.0",
]

[tool.uv]
package = false
